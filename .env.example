# ---------------------------------------------------------------------------
# Store Backend
# ---------------------------------------------------------------------------
# Supported values: memory, mongodb
#   memory  — no infrastructure needed, data lost on restart (default)
#   mongodb — local Docker or MongoDB Atlas
STORE_BACKEND=memory

# MongoDB connection (only used when STORE_BACKEND=mongodb)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DATABASE=northharbor_sage
# For Atlas, override MONGODB_URI via direnv / ~/.config/.env:
#   MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/?appName=sage

# ---------------------------------------------------------------------------
# Auth0
# ---------------------------------------------------------------------------
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_API_AUDIENCE=https://sage-api.northharbor.dev
AUTH0_ALGORITHMS=RS256

# ---------------------------------------------------------------------------
# LLM Provider
# ---------------------------------------------------------------------------
# Local default: Ollama
LLM_PROVIDER=ollama
LLM_MODEL=llama3.1:8b-instruct-q4_K_M
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_TIMEOUT_SECONDS=120

# OpenAI (alternative to local Ollama)
# LLM_PROVIDER=openai
# LLM_MODEL=gpt-4o-mini
# NORTHHARBOR_OPENAPI_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_TIMEOUT_SECONDS=45

# ---------------------------------------------------------------------------
# App
# ---------------------------------------------------------------------------
APP_ENV=development
LOG_LEVEL=info
